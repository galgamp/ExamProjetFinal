<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ExamFinal.Mobile.UI"
             x:Class="ExamFinal.Mobile.UI.MainPage"
             BindingContext="{Binding Main,Source={StaticResource Locator}}">

    <StackLayout>
        <!-- Place new controls here -->
        <Entry Text="{Binding EntryAdresse}" HorizontalOptions="CenterAndExpand"
           VerticalOptions="CenterAndExpand"/>
        <Button Command="{Binding AdresseCommand}"  HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand"/>
        <Label Text="{Binding LabelCoordonnee}" 
           HorizontalOptions="Center"
           VerticalOptions="CenterAndExpand" />
    </StackLayout>

</ContentPage>
